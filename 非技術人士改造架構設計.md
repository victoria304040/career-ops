# career-ops 非技術人士改造 — 架構設計文件

日期：2026-09-07
目標：讓「完全不懂程式、不會用終端機」的人，也能用 career-ops 掃職缺、評估、客製化 CV、追蹤應徵。

---

## 0. 核心結論（先講重點）

**這個專案已經有 80% 的網頁版了。** 我們不是「從零做一個網頁」，而是「把現有的 `web/` 網頁版，補上三個缺口，讓非技術人士能一鍵啟動」。

三個缺口：

| # | 缺口 | 現況 | 解法 |
|---|---|---|---|
| 1 | **啟動太技術** | 要 `cd web && npm ci && npm run dev` | 做一個「雙擊啟動器」：`.bat`（Windows）/ `.command`（Mac），自動檢查環境、裝依賴、開瀏覽器 |
| 2 | **評估綁 AI CLI** | `/api/run` 靠 `spawnHeadlessCli` 呼叫 Claude Code 等 CLI | 把現成的 `gemini-eval.mjs` / `openai-eval.mjs` / `ollama-eval.mjs` 接進網頁，讓使用者選「雲端 key」或「本機免費模型」 |
| 3 | **要裝 Node 22+** | `web/package.json` 要求 Node ≥ 22 | 啟動器自動偵測/引導安裝，或最終打包成免安裝版 |

---

## 1. 現有架構（改造的基礎）

### 1.1 兩層資料契約（不可破壞）

```
系統層（system files）          使用者層（user files）
├─ modes/  scripts/  templates/  ├─ cv.md（你的履歷）
├─ dashboard/  web/              ├─ config/（設定）
└─ providers/                    ├─ data/pipeline.md（職缺管線）
                                 ├─ data/applications.md（應徵追蹤）
                                 └─ reports/（評估報告）
```

**鐵律**：`web/` 是「使用者檔案的視圖」，不是第二套引擎。它讀寫的就是 CLI 用的同一批 Markdown 檔。任何改造都**不能**另建資料庫或平行引擎。

### 1.2 資料流

```
scan.mjs → data/pipeline.md → 評估(oferta.md + cv.md) → reports/NNN-*.md
                                        ↓
                              data/applications.md（追蹤）
                                        ↓
                              apply（人審核 + 點擊，永不自動送出）
```

### 1.3 現有網頁版（web/）已具備

- **技術棧**：Next.js 16 + React 19 + Tailwind 4 + TypeScript
- **頁面**：Pipeline（追蹤表）、Explore（掃描）、Apply（應徵）、CV、Config（設定）、Analytics、Today
- **API**：30+ 個 route（`/api/run`、`/api/status`、`/api/doctor`、`/api/explore`…）
- **安全**：`origin-guard.mjs` 做同源 + loopback 防護；apply 永不自動送出
- **AI 後端 UI 骨架**：`config-form.tsx` 已有 `PROVIDERS = [anthropic, openai, google, openrouter]` 和「key」模式，但**註解寫明「尚未接線」**

### 1.4 現成的獨立評估器（關鍵突破口）

專案已有三個**不需要 AI CLI** 的評估器，直接呼叫 API 打分：

| 檔案 | 後端 | 需要什麼 |
|---|---|---|
| `gemini-eval.mjs` | Google Gemini | `GEMINI_API_KEY` |
| `openai-eval.mjs` | 任何 OpenAI 相容端點（OpenAI/OpenRouter/Groq/DeepSeek/本機 LM Studio/Ollama /v1） | `OPENAI_API_KEY` + `OPENAI_BASE_URL` + `OPENAI_MODEL` |
| `ollama-eval.mjs` | 本機 Ollama | 裝 Ollama + pull 模型（**免費、完全離線**） |

這三個都讀 `modes/oferta.md` + `modes/_shared.md` + `cv.md`，輸出格式與 Claude 管線一致。**這就是「去 AI CLI 化」的現成答案。**

---

## 2. 目標架構（改造後）

```
┌─────────────────────────────────────────────────────┐
│  非技術使用者：雙擊「啟動求職助手.bat」                 │
│    ↓                                                  │
│  啟動器（launcher）自動：                              │
│    1. 檢查 Node ≥ 22（沒有就引導安裝）                 │
│    2. npm ci（第一次）                                │
│    3. 啟動 web 伺服器（localhost:3000）               │
│    4. 自動開瀏覽器                                    │
├─────────────────────────────────────────────────────┤
│  瀏覽器網頁 UI（現有 web/，補上 AI 後端接線）           │
│  ├─ 儀表板 / Pipeline / Explore / Apply / CV / Config │
│  └─ Config 頁：選 AI 後端（雲端 key 或本機 Ollama）    │
├─────────────────────────────────────────────────────┤
│  後端（現有 web/ API + 新增 eval 接線）                │
│  ├─ /api/run  → 現有（走 AI CLI，保留給進階使用者）    │
│  ├─ /api/eval → 新增（走 gemini/openai/ollama-eval）  │
│  └─ 資料仍存本機 data/、reports/（不變）               │
└─────────────────────────────────────────────────────┘
```

---

## 3. 三個改造模組的詳細設計

### 模組 A：雙擊啟動器（launcher）

**目標**：非技術人士雙擊一個檔案就能用。

**檔案**：
- `launcher/start.bat`（Windows）
- `launcher/start.command`（Mac）
- `launcher/start.sh`（Linux）

**start.bat 邏輯**（偽碼）：

```bat
@echo off
:: 1. 檢查 node
where node >nul 2>nul
if errorlevel 1 (
  echo 未偵測到 Node.js，正在開啟下載頁面...
  start https://nodejs.org/
  pause
  exit /b 1
)

:: 2. 檢查版本 ≥ 22
:: 3. 第一次執行：cd web && npm ci
:: 4. 啟動伺服器 + 開瀏覽器
cd web
start "" http://localhost:3000
npm run dev
```

**設計要點**：
- 用 `where node` 偵測，沒有就開 Node 官網下載頁（附中文說明）
- 版本檢查用 `node -e "process.exit(process.versions.node.split('.')[0] < 22 ? 1 : 0)"`
- 第一次跑 `npm ci`（之後可跳過，用一個 `.installed` 標記檔判斷）
- 啟動後延遲 2 秒再開瀏覽器（等伺服器起來）

**進階（第二階段）**：用 `pkg` 或 `nexe` 把整個東西打包成單一 `.exe`，連 Node 都不用裝。但這會遇到 Playwright 的 Chromium 下載問題，列為後續。

### 模組 B：AI 後端接線（去 AI CLI 化）

**目標**：讓使用者在 Config 頁選「雲端 key」或「本機免費模型」，不用裝任何 AI CLI。

**新增檔案**：
- `web/src/app/api/eval/route.ts` — 新的評估端點
- `web/src/lib/eval-backend.ts` — 包裝三個評估器的呼叫邏輯

**設計**：

```
POST /api/eval
  body: { backend: "gemini" | "openai" | "ollama", input: "<JD 文字>" }

後端邏輯：
  1. 讀 config 裡的 key / model 設定
  2. 依 backend 呼叫對應的 .mjs（用 child_process spawn）
     - gemini  → node gemini-eval.mjs "<JD>"
     - openai  → node openai-eval.mjs --url ... --model ... "<JD>"
     - ollama  → node ollama-eval.mjs --model ... "<JD>"
  3. 解析輸出（<<offer:>> 信封），寫入 reports/
  4. 回傳評估結果給前端顯示
```

**Config 頁改造**（`config-form.tsx`）：
- 把現有「key/manual 尚未接線」的死面板，接上真實邏輯
- 三種後端選項：
  - **Google Gemini**：填 `GEMINI_API_KEY`（有免費額度）
  - **OpenAI 相容**：填 key + base URL + model（進階）
  - **本機 Ollama**：偵測 `http://localhost:11434`，列出已 pull 的模型讓使用者選（**完全免費離線**）

**安全考量**（沿用專案鐵律）：
- key 存本機 `.env`，絕不上傳
- 評估結果寫入 `reports/`，走現有的單一寫入路徑
- 不自動送出任何東西

### 模組 C：降低環境門檻

**目標**：讓「裝 Node」這件事盡量無痛。

**分層策略**：

| 階段 | 做法 | 門檻 |
|---|---|---|
| 第一階段 | 啟動器自動偵測 + 引導安裝 Node | 使用者要自己裝 Node（點下一步） |
| 第二階段 | 打包成免安裝 `.exe`（pkg/nexe） | 零門檻，但 Playwright Chromium 要額外處理 |
| 第三階段 | 桌面 App（Tauri，體積小） | 最像一般軟體，開發較重 |

**建議**：先做第一階段（啟動器），驗證「非技術人士真的能用」後，再評估要不要上第二/三階段。

---

## 4. 實作順序（Roadmap）

### Phase 1：最小可用（MVP）— 讓非技術人士能跑起來

1. **寫啟動器** `launcher/start.bat`（+ Mac/Linux 版）
2. **接線 AI 後端**：新增 `/api/eval` + 改 `config-form.tsx`，讓使用者能選 Gemini/Ollama
3. **驗證**：找一個非技術人士，從零開始雙擊啟動 → 掃描 → 評估 → 看報告

### Phase 2：體驗打磨

4. 啟動器加「環境健康檢查」畫面（重用 `doctor.mjs`）
5. Config 頁加「測試連線」按鈕（驗證 key 有效）
6. 中文化關鍵介面（現有 UI 是英文）

### Phase 3：零門檻打包（選配）

7. 打包免安裝 `.exe`
8. 或升級 Tauri 桌面 App

---

## 5. 風險與注意事項

| 風險 | 說明 | 對策 |
|---|---|---|
| **破壞兩層契約** | 若另建資料庫，會跟 CLI 打架 | 嚴格走現有 Markdown 檔 + 單一寫入路徑 |
| **Playwright 下載** | `postinstall` 會下載 Chromium（~150MB），輕量筆電可能慢 | 啟動器提示「首次安裝需下載瀏覽器核心」 |
| **記憶體** | 評估是單次 API 呼叫，不載入大檔，符合全域規範 | 沿用現有串流/分批設計 |
| **key 安全** | 使用者 key 存本機 `.env` | 明確告知「key 只存你電腦，不上傳」 |
| **Ollama 效果** | 本機小模型評估品質可能較弱 | 預設推薦 Gemini 免費額度，Ollama 當離線備援 |

---

## 6. 需要你確認的決策點

1. **預設 AI 後端**：建議預設「Google Gemini（免費額度）」，Ollama 當離線選項。你同意嗎？
2. **中文化**：現有 UI 是英文，要不要在 Phase 2 做中文化？（Phase 1 先不做，聚焦「能跑」）
3. **打包目標**：第一階段只做啟動器（使用者要裝 Node），還是直接衝免安裝 `.exe`？
4. **範圍**：這次改造是「fork 後自己改」，還是「想回饋上游（開 PR）」？這會影響程式碼風格（上游有嚴格測試規範）。

---

## 附錄：關鍵檔案索引

| 檔案 | 用途 |
|---|---|
| `web/src/app/api/run/route.ts` | 現有評估端點（走 AI CLI） |
| `web/src/app/api/doctor/route.ts` | 環境健康檢查（重用 `doctor.mjs`） |
| `web/src/components/config-form.tsx` | 設定頁（AI 後端 UI 骨架，待接線） |
| `web/src/lib/clis.ts` | CLI 偵測與解析 |
| `web/src/lib/core/` | 核心存取層（pipeline、tracker、scan） |
| `gemini-eval.mjs` / `openai-eval.mjs` / `ollama-eval.mjs` | 獨立評估器（去 AI CLI 化的關鍵） |
| `web/AGENTS.md` | web 開發三鐵律（必讀） |
